#include <stdio.h>
#include <string.h>

#define TAM 50

//função principal
int main(){

    char sentenca[TAM];
    printf("Frase da sentença dançante:");
    fgets(sentenca,TAM,stdin);

    int tamanho = strlen(sentenca);

    for(int i = 0;i < tamanho;i++)

}